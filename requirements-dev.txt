# ──────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt — installed ONLY in CI and on developer machines.
#
# Kept separate from requirements.txt so that:
#   * the production Docker image stays slim (no pytest, no httpx test client)
#   * CI can pin tooling versions independently of runtime versions
# ──────────────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27         # used by FastAPI's TestClient
